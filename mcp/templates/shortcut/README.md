# Shortcut MCP

Official hosted MCP (Streamable HTTP + OAuth). No API token.

- Endpoint: `https://mcp.shortcut.com/mcp`
- Docs: https://www.shortcut.com/help/integrations/mcp-server/
- Plugin: `nanlabs-integrations` (`mcp.json`)

Authenticate in the client on first connect.
