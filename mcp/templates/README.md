# MCP templates (docs-only)

These directories are **documentation stubs** for wiring MCP servers in AI clients.

They are **not** bundled into marketplace plugins (`nanlabs-core`, `nanlabs-agents`, etc.). Installing those plugins does **not** install MCP integrations.

Optional integration plugins may ship later; until then treat this tree as contributor reference only.
