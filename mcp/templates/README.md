# MCP templates

Public MCP config **stubs** (env-var placeholders only) will land here.

Rules:

- Never commit tokens, PATs, bastion hosts, or private URLs.
- Prefer `${ENV_VAR}` substitution documented in each template README.
