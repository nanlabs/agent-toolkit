# Agents

Canonical agent/subagent personas will live here after the P0 extract (`agent-toolkit#7`) and Wave 1 migration.

P0 ships setup guidance via the `nanlabs-setup` **skill** and `/setup` command only.
